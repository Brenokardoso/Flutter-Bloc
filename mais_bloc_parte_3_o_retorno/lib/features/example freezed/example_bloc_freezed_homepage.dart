import 'package:flutter/material.dart';

class FreezedBlocPage extends StatelessWidget {
  const FreezedBlocPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Example Freezed"),
      ),
      body: Container(),
    );
  }
}
